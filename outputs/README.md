# 생성 산출물 영역

스크립트를 다시 실행해 만들 수 있는 결과는 Git에 커밋하지 않는다.

| 폴더 | 용도 |
| --- | --- |
| `validation/` | 품질·결합률 JSON과 로그 |
| `models/` | 학습 모델, 예측값, 평가표 |
| `figures/` | 레시피와 신청서에 사용할 그림 후보 |

검토가 끝난 핵심 수치와 해석만 `docs/kor` 문서로 옮긴다.
